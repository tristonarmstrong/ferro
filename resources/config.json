{
  "model": "qwen2.5:0.5b",
  "address": "127.0.0.1",
  "port": "11434",
  "commit": {
    "change_type": ["feat", "bug", "hotfix", "doc", "chore", "misc"],
    "instructions": "Create a commit message that describes a code change. Use one of the following change types: feat, bug, hotfix, doc, chore, misc.",
    "constraints": "Follow semantic commit style (e.g., `type: description`). Keep the message short, concise, and clear. Output only the commit message, nothing else."
  },
  "pull_request": {
    "change_type": [],
    "instructions": "",
    "constraints": ""
  }
}
